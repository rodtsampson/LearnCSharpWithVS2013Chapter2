using System;

namespace ValueAndReferenceCopy
{
    class Program
    {
        //static void Main()
        //{
        //    int x = 5;//1. Sets value of x equal to 5
        //    int y = x;//2. This line makes a copy of the data at x and store the copy to the y variable
        //    Console.WriteLine("x={0}, y={1}", x, y);//3. Prints values of x and y to screen
        //    x = 25;//4. Changes the value of x but does not automatically change the value of y
        //    Console.WriteLine("x={0}, y={1}", x, y);

        //    double[] arr1 = { 1, 2 };
        //    double[] arr2 = arr1;//5. Copy a reference from arr1 to arr2
        //    arr2[0] = 10;//6. Assings a new value to the array through arr2 at index 0
        //    Console.WriteLine("The value at index 0 is {0} ", arr1[0]);//7. Should also show 10 to the screen
        //}
    }
}